/**
 * Press features (plan §6, Block 5 Part C; D7 and §8.11).
 *
 * A feature is only shown once the clinic has supplied a link to it, so this
 * list stays empty until §11.2 is answered and the press row simply does not
 * render. Each entry takes { id, name, href, issue }.
 */
export const pressFeatures = [];
