/**
 * Colours for the 3D scene, taken from the `sanctuary` tokens in
 * tailwind.config.js so the stage and the page are one palette. Anything the
 * scene needs that the page does not (the serum) is defined here too, rather
 * than as a hex value inside a component.
 */
export const alabaster = '#F9F6F0';
export const pearl = '#F2EDE4';
export const stone = '#E8E1D5';
export const night = '#1A1817';
export const gold = '#C5A880';
export const goldLight = '#E5C992';
export const goldDeep = '#9E7E56';
export const clay = '#A8766B';
export const clayLight = '#CBA595';

/** The serum: a warm, pale amber that reads as oil-based skincare, not whisky. */
export const serum = '#DDB173';

/** The travertine plinth. */
export const travertine = '#F1EEE9';
